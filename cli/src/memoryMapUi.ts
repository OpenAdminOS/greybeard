/** A local, dependency-free perspective view of recorded memory relationships. */
export const memoryMapStyles = `
.map-heading{display:flex;justify-content:space-between;align-items:center;gap:20px;margin:20px 0 18px}.map-heading h1{font-size:32px;margin:0 0 5px}.map-heading .lead{margin:0;font-size:13px}.map-controls{display:flex;gap:12px;flex-wrap:wrap;align-items:end;margin:0 0 12px}.map-controls label{margin:0;display:grid;gap:5px;font-size:12px;color:var(--muted)}.map-controls label:first-child{flex:1}.map-controls input{width:100%}.map-controls select{max-width:180px}#map-feedback{margin:8px 0 12px;min-height:1.5em}.map-stage{position:relative;border-radius:16px;background:radial-gradient(ellipse at 50% 45%,#25354b 0%,#151e30 48%,#0e1421 100%);color:#e8edf6;overflow:hidden;border:1px solid #303e53}.map-stage svg{display:block;width:100%;height:clamp(440px,68vh,850px);touch-action:none;cursor:grab;outline-offset:-5px}.map-stage svg:active{cursor:grabbing}.map-stage svg:focus-visible{outline:3px solid #75e3d5}.map-stage:has(#map-empty:not([hidden])) svg{display:none}.map-stage-head{position:absolute;inset:20px 24px auto;display:flex;justify-content:space-between;gap:12px;font-size:12px;color:#acbbd0;pointer-events:none}.map-stage-head strong{font-weight:500;color:#eff6ff}.map-stage-foot{position:absolute;inset:auto 24px 116px;pointer-events:none;font-size:12px;color:#acbbd0}.map-empty{padding:100px 24px;text-align:center;color:#acbbd0}.map-timeline{padding:14px 24px 18px;border-top:1px solid #303e53;display:grid;gap:5px;background:#0e142150}.map-timeline input{width:100%;accent-color:#75e3d5;padding:0}.map-timeline label{display:flex;justify-content:space-between;gap:16px;font-size:12px;color:#c2cddd;margin:0}.map-under{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:12px;margin:14px 0}.map-legend{display:flex;flex-wrap:wrap;gap:18px;font-size:12px;color:var(--muted)}.map-legend span:before{content:'';display:inline-block;width:9px;height:9px;border-radius:50%;background:#168c83;margin-right:7px}.map-legend .proposed:before{background:#ed765f;border:1px dashed #863827}.map-legend .archived:before{background:#7a8497}.map-small-actions{display:flex;gap:8px}.map-small-actions button{font-size:12px;padding:8px 12px}.map-index{border:1px solid var(--line);border-radius:9px;padding:16px;background:#fffdf8}.map-index summary{cursor:pointer;font-size:13px}.map-index label{font-size:12px;color:var(--muted)}.map-index select{width:100%;max-width:none;min-height:165px}.map-index option{padding:8px}.map-index button{margin-top:12px}.map-flat .map-stage svg,.map-flat .map-stage-head,.map-flat .map-stage-foot{display:none}.map-flat .map-index{margin-top:16px}.map-detail{width:min(640px,calc(100vw - 40px));max-height:85vh;padding:28px;overflow:auto}.map-detail::backdrop{background:#080f20aa;backdrop-filter:blur(5px)}.map-detail-head{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-bottom:18px}.map-detail-head button{font-size:13px}.map-detail h2{font-size:27px;margin:0 0 16px}.map-detail .map-exact{font-size:18px;line-height:1.65;white-space:pre-wrap;overflow-wrap:anywhere;max-height:none}.map-detail dl{display:grid;grid-template-columns:auto 1fr;gap:10px 18px;font-size:13px}.map-detail dt{color:var(--muted)}.map-detail dd{margin:0;overflow-wrap:anywhere}.map-detail .map-links{display:grid;gap:8px;margin:22px 0}.map-links button{text-align:left;font-size:13px}.map-detail details{font-size:12px;color:var(--muted);margin-top:16px}.map-note{max-width:85ch}#view-map .foot{margin-top:16px}
@media(max-width:700px){.map-heading{align-items:start}.map-heading h1{font-size:28px}.map-controls label:first-child{flex-basis:100%}.map-stage svg{height:480px}.map-stage-head{inset:18px 16px auto}.map-stage-foot{inset:auto 16px 115px;font-size:11px}.map-under{align-items:start}.map-detail{padding:22px}}
`;

export const memoryMapMarkup = `
<section class="view" id="view-map" hidden>
<div class="map-heading"><div><h1>Memory map.</h1><p class="lead">Follow a connection. Click a memory to see its story.</p></div><button id="map-refresh">Refresh</button></div>
<div class="map-controls"><label>Find a memory<input id="map-query" type="search" placeholder="Search your memories"></label><label>Status<select id="map-status"><option value="">Every status</option><option value="confirmed">Confirmed</option><option value="candidate">Needs review</option><option value="superseded">Superseded</option></select></label><label>Show up to<select id="map-limit"><option value="200">200 memories</option><option value="500">500 memories</option><option value="1000">1,000 memories</option></select></label></div>
<p id="map-feedback" class="note" role="status" aria-live="polite">Loading your memories…</p>
<div class="map-stage"><div class="map-stage-head"><span>YOUR EXPERIENCE, CONNECTED</span><strong id="map-count">0 memories</strong></div><svg id="memory-globe" viewBox="0 0 1000 760" tabindex="0" role="img" aria-label="Memory globe. Click a memory to open its details. Drag or use arrow keys to rotate; press Enter to inspect a memory. A keyboard memory list is available below."><title>Recorded memory connections</title></svg><div id="map-empty" class="map-empty" hidden>No memories to show yet. Add a memory or adjust your search.</div><div class="map-stage-foot">Drag to explore · Click a memory for details</div><div class="map-timeline"><label for="map-time"><span>Saved through</span><output id="map-time-label">All time</output></label><input id="map-time" type="range" min="0" max="100" value="100" step="1" aria-label="Show memories saved through this point in time"></div></div>
<div class="map-under"><div class="map-legend"><span>Confirmed</span><span class="proposed">Needs review</span><span class="archived">Superseded</span></div><div class="map-small-actions"><button id="map-reset">Reset view</button><button id="map-flat" aria-pressed="false">Flat view</button></div></div>
<details class="map-index" id="map-index"><summary>Browse memories as a list</summary><label for="map-select">Select a memory · newest first</label><select id="map-select" size="7" aria-label="Memories in the map"></select><button id="map-inspect">Open selected memory</button></details>
<dialog id="map-detail" class="map-detail" aria-labelledby="map-detail-title"><div class="map-detail-head"><span id="map-detail-state" class="eyebrow"></span><button id="map-close">Close details</button></div><h2 id="map-detail-title">Memory details</h2><div id="map-detail-body"></div></dialog>
<p class="foot map-note">Connections show recorded relationships and corrections. Position and distance are visual layout, not evidence of similarity. Only confirmed, applicable memories inform advice. This map runs locally without model calls.</p>
</section>`;

export const memoryMapScript = String.raw`
let mapData=null,mapSelected=null,mapYaw=.3,mapPitch=-.2,mapFlat=false,mapLoading=0,mapSearchTimer,mapFrame=0,mapDrag=null,mapPositions=new Map();
const mapSvgNS='http://www.w3.org/2000/svg';
function mapDate(value){return value!==null&&value!==undefined?new Date(value*1000).toLocaleString():'Not recorded';}
function mapStatus(node){return node.supersededAt!==null?'Superseded':node.status==='candidate'?'Needs review':'Confirmed';}
function mapSvg(tag,attrs={},text){const element=document.createElementNS(mapSvgNS,tag);for(const [key,value]of Object.entries(attrs))element.setAttribute(key,String(value));if(text!==undefined)element.textContent=text;return element;}
async function loadMemoryMap(){const generation=++mapLoading;$('map-feedback').textContent='Loading local memories…';try{const data=await api('/memory-map',{query:$('map-query').value,status:$('map-status').value,limit:Number($('map-limit').value)});if(generation!==mapLoading)return;mapData=data;$('map-feedback').textContent='Showing '+data.nodes.length+' of '+data.matched+' matching memories'+(data.nodes.length<data.matched?' · narrow the search or increase the limit':'')+'. Updated '+new Date().toLocaleTimeString()+'.';if(!data.nodes.some(n=>n.id===mapSelected))mapSelected=null;mapUpdate();}catch(error){if(generation===mapLoading){mapData=null;mapSelected=null;mapUpdate();$('map-feedback').textContent='Could not load this memory map. '+error.message;}}}
function mapVisible(){if(!mapData)return [];const value=Number($('map-time').value);const earliest=mapData.earliest||0,latest=mapData.latest||earliest;const cutoff=value===100?Infinity:earliest+(latest-earliest)*value/100;$('map-time-label').textContent=value===100?'All time':mapDate(cutoff);return mapData.nodes.filter(n=>n.createdAt<=cutoff);}
function mapUpdate(){const nodes=mapVisible();$('map-count').textContent=nodes.length+' memories';$('map-empty').hidden=nodes.length>0;$('map-time').disabled=!mapData?.nodes.length||mapData.earliest===mapData.latest;$('map-select').replaceChildren();for(const node of nodes){const option=el('option','#'+node.id+' · '+mapStatus(node)+' · '+node.content.slice(0,110));option.value=String(node.id);$('map-select').append(option);}if(!nodes.some(n=>n.id===mapSelected))mapSelected=null;$('map-select').value=mapSelected===null?'':String(mapSelected);$('map-inspect').disabled=mapSelected===null;mapDetails();mapDraw();}
function mapProject(point){const cy=Math.cos(mapYaw),sy=Math.sin(mapYaw),cp=Math.cos(mapPitch),sp=Math.sin(mapPitch);const x=point[0]*cy+point[2]*sy,z=-point[0]*sy+point[2]*cy;const y=point[1]*cp-z*sp,depth=point[1]*sp+z*cp;const scale=820/(820-depth*210);return {x:500+x*300*scale,y:380+y*300*scale,z:depth,scale};}
function mapPath(points){return points.map((p,i)=>{const q=mapProject(p);return(i?'L':'M')+q.x.toFixed(1)+','+q.y.toFixed(1);}).join(' ');}
function mapDraw(){if($('view-map').hidden)return;const svg=$('memory-globe');svg.replaceChildren(mapSvg('title',{},'Recorded memory connections'));mapPositions=new Map();const nodes=mapVisible();if(!nodes.length)return;
const grid=mapSvg('g',{fill:'none',stroke:'#8cafd6','stroke-width':.65,opacity:.16});for(const latitude of [-.65,0,.65]){const radius=Math.sqrt(1-latitude*latitude),points=[];for(let i=0;i<=80;i++){const a=i/80*Math.PI*2;points.push([Math.cos(a)*radius,latitude,Math.sin(a)*radius]);}grid.append(mapSvg('path',{d:mapPath(points)}));}for(let meridian=0;meridian<4;meridian++){const points=[];for(let i=0;i<=80;i++){const a=i/80*Math.PI*2,b=meridian*Math.PI/4;points.push([Math.cos(a)*Math.cos(b),Math.sin(a),Math.cos(a)*Math.sin(b)]);}grid.append(mapSvg('path',{d:mapPath(points)}));}svg.append(grid);
const sorted=[...nodes].sort((a,b)=>a.createdAt-b.createdAt||a.id-b.id);for(let i=0;i<sorted.length;i++){const node=sorted[i],y=sorted.length===1?0:1-2*(i+.5)/sorted.length,angle=i*Math.PI*(3-Math.sqrt(5)),r=Math.sqrt(1-y*y);const point=[Math.cos(angle)*r,y,Math.sin(angle)*r];mapPositions.set(node.id,{...mapProject(point),node,point});}
const lines=mapSvg('g',{'pointer-events':'none'});
for(const edge of mapData.edges){const a=mapPositions.get(edge.source),b=mapPositions.get(edge.target);if(!a||!b)continue;const selected=edge.source===mapSelected||edge.target===mapSelected;lines.append(mapSvg('line',{x1:a.x,y1:a.y,x2:b.x,y2:b.y,stroke:edge.kind==='correction'?'#ffb98c':'#83aef5','stroke-width':selected?2:1,opacity:selected ? .95 : .38}));}svg.append(lines);
for(const p of [...mapPositions.values()].sort((a,b)=>a.z-b.z)){const selected=p.node.id===mapSelected,archived=p.node.supersededAt!==null,candidate=p.node.status==='candidate';const radius=(selected?10:7)*p.scale;const g=mapSvg('g',{'data-memory-id':p.node.id});g.append(mapSvg('title',{},'#'+p.node.id+' · '+mapStatus(p.node)+' · '+p.node.content.slice(0,140)));if(selected)g.append(mapSvg('circle',{cx:p.x,cy:p.y,r:radius+5,fill:'none',stroke:'#f2f7ff','stroke-width':1,opacity:.8}));g.append(mapSvg('circle',{cx:p.x,cy:p.y,r:radius*2.3,fill:candidate?'#ff947d':'#75e3d5',opacity:archived?0:.08}));g.append(mapSvg('circle',{cx:p.x,cy:p.y,r:radius,fill:archived?'#7a8497':candidate?'#ff947d':'#75e3d5',stroke:selected?'#ffffff':candidate?'#ffc2af':'#c0fff1','stroke-width':selected?2:1,'stroke-dasharray':candidate?'2 2':'none',opacity:archived ? .45 : .50+(p.z+1)*.25}));svg.append(g);}}
function mapSchedule(){if(!mapFrame)mapFrame=requestAnimationFrame(()=>{mapFrame=0;mapDraw();});}
function mapInspect(id){if(!mapVisible().some(n=>n.id===id))return;mapSelected=id;$('map-select').value=String(id);$('map-inspect').disabled=false;mapDetails();mapSchedule();if(!$('map-detail').open)$('map-detail').showModal();}
function mapDetails(){const panel=$('map-detail-body');panel.replaceChildren();const node=mapData?.nodes.find(n=>n.id===mapSelected);if(!node){if($('map-detail').open)$('map-detail').close();return;}$('map-detail-state').textContent=mapStatus(node)+' · #'+node.id;$('map-detail-title').textContent=({preference:'Operating preference',decision:'Decision & lesson',fact:'Observation',script:'Remembered script',query:'Remembered query',scope:'Memory scope'})[node.type]||'Memory details';panel.append(el('p',node.content,'map-exact'));const dl=el('dl');for(const [label,value]of [['Saved',mapDate(node.createdAt)],['Confirmed',mapDate(node.confirmedAt)],['Scope',node.scope]])dl.append(el('dt',label),el('dd',value));panel.append(dl);const provenance=el('details');provenance.append(el('summary','More about this memory'));const extra=el('dl');for(const [label,value]of [['Recorded source',node.source||'Not recorded'],['Last used',mapDate(node.lastUsedAt)],['Observed',mapDate(node.observedAt)],['Storage','This computer · '+(mapData.tenant==='local'?'local memory':'selected tenant profile')]])extra.append(el('dt',label),el('dd',value));provenance.append(extra);panel.append(provenance);if(node.outcome)panel.append(el('p','Reported outcome: '+node.outcome,'note'));const visibleIds=new Set(mapVisible().map(n=>n.id));const related=mapData.edges.filter(edge=>visibleIds.has(edge.source)&&visibleIds.has(edge.target)&&(edge.source===node.id||edge.target===node.id));const links=el('div',undefined,'map-links');links.append(el('strong','Recorded connections'));if(!related.length)links.append(el('p','No connected memories are included in this view.','note'));for(const edge of related){const other=edge.source===node.id?edge.target:edge.source;const correction=mapData.nodes.find(n=>n.id===edge.source);const label=edge.relation==='corrects'?(correction?.status==='candidate'?(edge.source===node.id?'Proposed correction to':'Proposed replacement'):(edge.source===node.id?'Corrects':'Corrected by')):edge.relation.replaceAll('_',' ')+(edge.source===node.id?' →':' ←');links.append(button(label+' #'+other,()=>mapInspect(other)));}panel.append(links,button('Open in Your memory',()=>{$('map-detail').close();$('search').value=node.content;$('type-filter').value='';$('status-filter').value='';$('scope-filter').value='';view('memory');}));}
$('map-refresh').onclick=()=>void loadMemoryMap();$('map-query').oninput=()=>{clearTimeout(mapSearchTimer);mapSearchTimer=setTimeout(()=>void loadMemoryMap(),250);};for(const id of ['map-status','map-limit'])$(id).onchange=()=>void loadMemoryMap();$('map-time').oninput=mapUpdate;$('map-select').onchange=()=>{mapSelected=Number($('map-select').value);$('map-inspect').disabled=false;mapSchedule();};$('map-inspect').onclick=()=>mapInspect(mapSelected);$('map-close').onclick=()=>$('map-detail').close();$('map-select').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();mapInspect(mapSelected);}};$('map-reset').onclick=()=>{mapYaw=.3;mapPitch=-.2;$('map-time').value='100';mapUpdate();};$('map-flat').onclick=()=>{mapFlat=!mapFlat;$('view-map').classList.toggle('map-flat',mapFlat);$('map-flat').setAttribute('aria-pressed',String(mapFlat));$('map-flat').textContent=mapFlat?'Globe view':'Flat view';$('map-index').open=mapFlat;};$('memory-globe').onpointerdown=event=>{if(event.button!==0)return;mapDrag={x:event.clientX,y:event.clientY,lastX:event.clientX,lastY:event.clientY,moved:false};$('memory-globe').setPointerCapture(event.pointerId);};$('memory-globe').onpointermove=event=>{if(!mapDrag)return;const dx=event.clientX-mapDrag.lastX,dy=event.clientY-mapDrag.lastY;mapDrag.moved=mapDrag.moved||Math.hypot(event.clientX-mapDrag.x,event.clientY-mapDrag.y)>4;if(mapDrag.moved){mapYaw+=dx*.008;mapPitch=Math.max(-1.4,Math.min(1.4,mapPitch+dy*.008));mapSchedule();}mapDrag.lastX=event.clientX;mapDrag.lastY=event.clientY;};$('memory-globe').onpointerup=event=>{if(!mapDrag)return;if(!mapDrag.moved){const matrix=$('memory-globe').getScreenCTM();if(matrix){const point=new DOMPoint(event.clientX,event.clientY).matrixTransform(matrix.inverse());const candidates=[...mapPositions.values()].filter(p=>Math.hypot(p.x-point.x,p.y-point.y)<14).sort((a,b)=>b.z-a.z);if(candidates.length){mapInspect(candidates[0].node.id);}}}mapDrag=null;};$('memory-globe').onpointercancel=()=>{mapDrag=null;};$('memory-globe').onkeydown=event=>{if(event.key==='Enter'){event.preventDefault();const first=[...mapPositions.values()].sort((a,b)=>b.z-a.z)[0];mapInspect(mapSelected??first?.node.id);return;}if(!['ArrowLeft','ArrowRight','ArrowUp','ArrowDown','Home'].includes(event.key))return;event.preventDefault();if(event.key==='Home'){mapYaw=.3;mapPitch=-.2;}else if(event.key==='ArrowLeft')mapYaw-=.15;else if(event.key==='ArrowRight')mapYaw+=.15;else mapPitch=Math.max(-1.4,Math.min(1.4,mapPitch+(event.key==='ArrowUp'?-.15:.15)));mapSchedule();};
`;
