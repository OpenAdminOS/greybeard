# Independent review decisions

Accepted: prove the mentor earlier; separate candidates from human-confirmed lessons; preserve scope and corrections; bound expanded recall; replace checkout-dependent adapters; default updates to Notify.

Retained: free public 0.1, original beard logo, one executable per platform, graphical and terminal setup, optional customer-owned app-only access. Terminal-first engineering does not remove graphical setup from the release plan.

Corrected: the HTML concept already renders the beard logo via CSS. Excess application grants already keep a profile inactive. Twenty unrelated commands are an initial fixture, not proof of zero false warnings.

The full external review and HTML feedback are private Downloads artifacts. Technical claims about authentication, host APIs, and minimum permissions require separate verification. No product release, certificate setup, tenant change, or external publication is implied by beginning implementation.
