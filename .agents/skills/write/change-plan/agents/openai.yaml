interface:
  display_name: "Change Plan"
  short_description: "Prepare a tenant change and recovery plan"
  default_prompt: "Use $change-plan to prepare this tenant change for my review without executing it."
